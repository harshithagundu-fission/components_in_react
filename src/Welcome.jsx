//learning the mounting
//understanding the mount/ render in react
//mount --> wehn the component is visible for the 1st time on the screen . 1st time mount+render
//render --> when the function in the component executes every time (when a button is clicked everytime, the function will execute and the component will re-render)


function Welcome(){
    return(
        <h1>Hello World!</h1>
    );
}
export default Welcome;